import React from 'react';
import ReactDOM from 'react-dom/client';
import Products from './components/ListItem/ProductListItem';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <div>
        <Products />
    </div>
);